[project]
name = "demo-repo"
version = "0.1.0"
requires-python = ">=3.12, <3.14"
dependencies = [
    "cocotb",
    "cocotb-bus",
    "pytest",
]

[project.optional-dependencies]
dev = [
    "pytest-xdist",
    "cocotb-test",
]
