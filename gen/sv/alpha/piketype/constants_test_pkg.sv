// Generated by piketype
// Source: alpha/piketype/constants.py
// Do not edit by hand.

package constants_test_pkg;
  import constants_pkg::*;

  class addr_ct;
    localparam int WIDTH = LP_ADDR_WIDTH;
    localparam int BYTE_COUNT = LP_ADDR_BYTE_COUNT;
    rand addr_t value;
  
    function new(addr_t value_in = '0);
      value = value_in;
    endfunction
  
    function automatic addr_t to_slv();
      return value;
    endfunction
  
    function void from_slv(addr_t value_in);
      value = value_in;
    endfunction
  
    task automatic to_bytes(output byte unsigned bytes[]);
      logic [15:0] padded;
      bytes = new[BYTE_COUNT];
      padded = '0;
      padded[WIDTH-1:0] = value;
      for (int idx = 0; idx < BYTE_COUNT; idx++) begin
        bytes[idx] = padded[(BYTE_COUNT - 1 - idx)*8 +: 8];
      end
    endtask
  
    function void from_bytes(input byte unsigned bytes[]);
      logic [15:0] padded;
      if (bytes.size() != BYTE_COUNT) begin
        $fatal(1, "addr_ct.from_bytes size mismatch: expected %0d got %0d", BYTE_COUNT, bytes.size());
      end
      padded = '0;
      for (int idx = 0; idx < BYTE_COUNT; idx++) begin
        padded[(BYTE_COUNT - 1 - idx)*8 +: 8] = bytes[idx];
      end
      value = addr_t'(padded[WIDTH-1:0]);
    endfunction
  
    function void copy(input addr_ct rhs);
      value = rhs.value;
    endfunction
  
    function automatic addr_ct clone();
      addr_ct cloned = new();
      cloned.value = value;
      return cloned;
    endfunction
  
    function automatic bit compare(input addr_ct rhs);
      return value === rhs.value;
    endfunction
  
    function automatic string sprint();
      return $sformatf("addr_ct(value=0x%0h)", value);
    endfunction
  endclass : addr_ct

  class mask_ct;
    localparam int WIDTH = LP_MASK_WIDTH;
    localparam int BYTE_COUNT = LP_MASK_BYTE_COUNT;
    rand mask_t value;
  
    function new(mask_t value_in = '0);
      value = value_in;
    endfunction
  
    function automatic mask_t to_slv();
      return value;
    endfunction
  
    function void from_slv(mask_t value_in);
      value = value_in;
    endfunction
  
    task automatic to_bytes(output byte unsigned bytes[]);
      logic [7:0] padded;
      bytes = new[BYTE_COUNT];
      padded = '0;
      padded[WIDTH-1:0] = value;
      for (int idx = 0; idx < BYTE_COUNT; idx++) begin
        bytes[idx] = padded[(BYTE_COUNT - 1 - idx)*8 +: 8];
      end
    endtask
  
    function void from_bytes(input byte unsigned bytes[]);
      logic [7:0] padded;
      if (bytes.size() != BYTE_COUNT) begin
        $fatal(1, "mask_ct.from_bytes size mismatch: expected %0d got %0d", BYTE_COUNT, bytes.size());
      end
      padded = '0;
      for (int idx = 0; idx < BYTE_COUNT; idx++) begin
        padded[(BYTE_COUNT - 1 - idx)*8 +: 8] = bytes[idx];
      end
      value = mask_t'(padded[WIDTH-1:0]);
    endfunction
  
    function void copy(input mask_ct rhs);
      value = rhs.value;
    endfunction
  
    function automatic mask_ct clone();
      mask_ct cloned = new();
      cloned.value = value;
      return cloned;
    endfunction
  
    function automatic bit compare(input mask_ct rhs);
      return value === rhs.value;
    endfunction
  
    function automatic string sprint();
      return $sformatf("mask_ct(value=0x%0h)", value);
    endfunction
  endclass : mask_ct

  class flag_ct;
    localparam int WIDTH = LP_FLAG_WIDTH;
    localparam int BYTE_COUNT = LP_FLAG_BYTE_COUNT;
    rand flag_t value;
  
    function new(flag_t value_in = '0);
      value = value_in;
    endfunction
  
    function automatic flag_t to_slv();
      return value;
    endfunction
  
    function void from_slv(flag_t value_in);
      value = value_in;
    endfunction
  
    task automatic to_bytes(output byte unsigned bytes[]);
      logic [7:0] padded;
      bytes = new[BYTE_COUNT];
      padded = '0;
      padded[WIDTH-1:0] = value;
      for (int idx = 0; idx < BYTE_COUNT; idx++) begin
        bytes[idx] = padded[(BYTE_COUNT - 1 - idx)*8 +: 8];
      end
    endtask
  
    function void from_bytes(input byte unsigned bytes[]);
      logic [7:0] padded;
      if (bytes.size() != BYTE_COUNT) begin
        $fatal(1, "flag_ct.from_bytes size mismatch: expected %0d got %0d", BYTE_COUNT, bytes.size());
      end
      padded = '0;
      for (int idx = 0; idx < BYTE_COUNT; idx++) begin
        padded[(BYTE_COUNT - 1 - idx)*8 +: 8] = bytes[idx];
      end
      value = flag_t'(padded[WIDTH-1:0]);
    endfunction
  
    function void copy(input flag_ct rhs);
      value = rhs.value;
    endfunction
  
    function automatic flag_ct clone();
      flag_ct cloned = new();
      cloned.value = value;
      return cloned;
    endfunction
  
    function automatic bit compare(input flag_ct rhs);
      return value === rhs.value;
    endfunction
  
    function automatic string sprint();
      return $sformatf("flag_ct(value=0x%0h)", value);
    endfunction
  endclass : flag_ct

  class big_data_ct;
    localparam int WIDTH = LP_BIG_DATA_WIDTH;
    localparam int BYTE_COUNT = LP_BIG_DATA_BYTE_COUNT;
    rand big_data_t value;
  
    function new(big_data_t value_in = '0);
      value = value_in;
    endfunction
  
    function automatic big_data_t to_slv();
      return value;
    endfunction
  
    function void from_slv(big_data_t value_in);
      value = value_in;
    endfunction
  
    task automatic to_bytes(output byte unsigned bytes[]);
      logic [71:0] padded;
      bytes = new[BYTE_COUNT];
      padded = '0;
      padded[WIDTH-1:0] = value;
      for (int idx = 0; idx < BYTE_COUNT; idx++) begin
        bytes[idx] = padded[(BYTE_COUNT - 1 - idx)*8 +: 8];
      end
    endtask
  
    function void from_bytes(input byte unsigned bytes[]);
      logic [71:0] padded;
      if (bytes.size() != BYTE_COUNT) begin
        $fatal(1, "big_data_ct.from_bytes size mismatch: expected %0d got %0d", BYTE_COUNT, bytes.size());
      end
      padded = '0;
      for (int idx = 0; idx < BYTE_COUNT; idx++) begin
        padded[(BYTE_COUNT - 1 - idx)*8 +: 8] = bytes[idx];
      end
      value = big_data_t'(padded[WIDTH-1:0]);
    endfunction
  
    function void copy(input big_data_ct rhs);
      value = rhs.value;
    endfunction
  
    function automatic big_data_ct clone();
      big_data_ct cloned = new();
      cloned.value = value;
      return cloned;
    endfunction
  
    function automatic bit compare(input big_data_ct rhs);
      return value === rhs.value;
    endfunction
  
    function automatic string sprint();
      return $sformatf("big_data_ct(value=0x%0h)", value);
    endfunction
  endclass : big_data_ct

  class flags_ct;
    localparam int WIDTH = 3;
    localparam int BYTE_COUNT = 1;
    rand logic invalid_data;
    rand logic timeout;
    rand logic overflow;
  
    function new();
      invalid_data = '0;
      timeout = '0;
      overflow = '0;
    endfunction
  
    function automatic flags_t to_slv();
      flags_t packed_value;
      packed_value.invalid_data = invalid_data;
      packed_value.timeout = timeout;
      packed_value.overflow = overflow;
      packed_value._align_pad = '0;
      return packed_value;
    endfunction
  
    function void from_slv(flags_t value_in);
      invalid_data = value_in.invalid_data;
      timeout = value_in.timeout;
      overflow = value_in.overflow;
    endfunction
  
    task automatic to_bytes(output byte unsigned bytes[]);
      logic [7:0] bv;
      bytes = new[BYTE_COUNT];
      bv = '0;
      bv[7] = invalid_data;
      bv[6] = timeout;
      bv[5] = overflow;
      for (int idx = 0; idx < BYTE_COUNT; idx++) begin
        bytes[idx] = bv[(BYTE_COUNT - 1 - idx)*8 +: 8];
      end
    endtask
  
    function void from_bytes(input byte unsigned bytes[]);
      logic [7:0] bv;
      if (bytes.size() != BYTE_COUNT) begin
        $fatal(1, "flags_ct.from_bytes size mismatch: expected %0d got %0d", BYTE_COUNT, bytes.size());
      end
      bv = '0;
      for (int idx = 0; idx < BYTE_COUNT; idx++) begin
        bv[(BYTE_COUNT - 1 - idx)*8 +: 8] = bytes[idx];
      end
      invalid_data = bv[7];
      timeout = bv[6];
      overflow = bv[5];
    endfunction
  
    function void copy(input flags_ct rhs);
      invalid_data = rhs.invalid_data;
      timeout = rhs.timeout;
      overflow = rhs.overflow;
    endfunction
  
    function automatic flags_ct clone();
      flags_ct cloned = new();
      cloned.copy(this);
      return cloned;
    endfunction
  
    function automatic bit compare(input flags_ct rhs);
      bit match;
      match = 1'b1;
      match &= (invalid_data === rhs.invalid_data);
      match &= (timeout === rhs.timeout);
      match &= (overflow === rhs.overflow);
      return match;
    endfunction
  
    function automatic string sprint();
      return $sformatf("flags_ct(invalid_data=%0b, timeout=%0b, overflow=%0b)", invalid_data, timeout, overflow);
    endfunction
  endclass : flags_ct

  class header_ct;
    localparam int WIDTH = LP_HEADER_WIDTH;
    localparam int BYTE_COUNT = LP_HEADER_BYTE_COUNT;
    rand addr_t addr;
    rand flag_t enable;
    rand big_data_t data;
    flags_ct status;
  
    function new();
      addr = '0;
      enable = '0;
      data = '0;
      status = new();
    endfunction
  
    function automatic header_t to_slv();
      header_t packed_value;
      packed_value.addr = addr;
      packed_value.addr_pad = '0;
      packed_value.enable = enable;
      packed_value.enable_pad = '0;
      packed_value.data = data;
      packed_value.data_pad = '0;
      packed_value.status = status.to_slv();
      return packed_value;
    endfunction
  
    function void from_slv(header_t value_in);
      addr = value_in.addr;
      enable = value_in.enable;
      data = value_in.data;
      status.from_slv(value_in.status);
    endfunction
  
    task automatic to_bytes(output byte unsigned bytes[]);
      int byte_idx;
      bytes = new[BYTE_COUNT];
      byte_idx = 0;
      begin
        logic [15:0] fb;
        fb = '0;
        fb[12:0] = addr;
        for (int i = 0; i < 2; i++) bytes[byte_idx + i] = fb[(2 - 1 - i)*8 +: 8];
        byte_idx += 2;
      end
      begin
        logic [7:0] fb;
        fb = '0;
        fb[0:0] = enable;
        for (int i = 0; i < 1; i++) bytes[byte_idx + i] = fb[(1 - 1 - i)*8 +: 8];
        byte_idx += 1;
      end
      begin
        logic [71:0] fb;
        fb = '0;
        fb[66:0] = data;
        for (int i = 0; i < 9; i++) bytes[byte_idx + i] = fb[(9 - 1 - i)*8 +: 8];
        byte_idx += 9;
      end
      begin
        byte unsigned field_bytes[];
        status.to_bytes(field_bytes);
        for (int i = 0; i < 1; i++) bytes[byte_idx + i] = field_bytes[i];
        byte_idx += 1;
      end
    endtask
  
    function void from_bytes(input byte unsigned bytes[]);
      int byte_idx;
      if (bytes.size() != BYTE_COUNT) begin
        $fatal(1, "header_ct.from_bytes size mismatch: expected %0d got %0d", BYTE_COUNT, bytes.size());
      end
      byte_idx = 0;
      begin
        logic [15:0] fb;
        fb = '0;
        for (int i = 0; i < 2; i++) fb[(2 - 1 - i)*8 +: 8] = bytes[byte_idx + i];
        addr = fb[12:0];
        byte_idx += 2;
      end
      begin
        logic [7:0] fb;
        fb = '0;
        for (int i = 0; i < 1; i++) fb[(1 - 1 - i)*8 +: 8] = bytes[byte_idx + i];
        enable = fb[0:0];
        byte_idx += 1;
      end
      begin
        logic [71:0] fb;
        fb = '0;
        for (int i = 0; i < 9; i++) fb[(9 - 1 - i)*8 +: 8] = bytes[byte_idx + i];
        data = fb[66:0];
        byte_idx += 9;
      end
      begin
        byte unsigned field_bytes[] = new[1];
        for (int i = 0; i < 1; i++) field_bytes[i] = bytes[byte_idx + i];
        status.from_bytes(field_bytes);
        byte_idx += 1;
      end
    endfunction
  
    function void copy(input header_ct rhs);
      addr = rhs.addr;
      enable = rhs.enable;
      data = rhs.data;
      status.copy(rhs.status);
    endfunction
  
    function automatic header_ct clone();
      header_ct cloned = new();
      cloned.copy(this);
      return cloned;
    endfunction
  
    function automatic bit compare(input header_ct rhs);
      bit match;
      match = 1'b1;
      match &= (addr === rhs.addr);
      match &= (enable === rhs.enable);
      match &= (data === rhs.data);
      match &= status.compare(rhs.status);
      return match;
    endfunction
  
    function automatic string sprint();
      return $sformatf("header_ct(addr=0x%0h, enable=0x%0h, data=0x%0h, status=%s)", addr, enable, data, status.sprint());
    endfunction
  endclass : header_ct

  class packet_ct;
    localparam int WIDTH = LP_PACKET_WIDTH;
    localparam int BYTE_COUNT = LP_PACKET_BYTE_COUNT;
    header_ct header;
    rand logic [1:0] mode;
    rand bit [2:0] error_code;
    rand logic [31:0] data1;
    rand logic [31:0] data2;
  
    function new();
      header = new();
      mode = '0;
      error_code = '0;
      data1 = '0;
      data2 = '0;
    endfunction
  
    function automatic packet_t to_slv();
      packet_t packed_value;
      packed_value.header = header.to_slv();
      packed_value.mode = mode;
      packed_value.mode_pad = '0;
      packed_value.error_code = error_code;
      packed_value.error_code_pad = '0;
      packed_value.data1 = data1;
      packed_value.data2 = data2;
      return packed_value;
    endfunction
  
    function void from_slv(packet_t value_in);
      header.from_slv(value_in.header);
      mode = value_in.mode;
      error_code = value_in.error_code;
      data1 = value_in.data1;
      data2 = value_in.data2;
    endfunction
  
    task automatic to_bytes(output byte unsigned bytes[]);
      int byte_idx;
      bytes = new[BYTE_COUNT];
      byte_idx = 0;
      begin
        byte unsigned field_bytes[];
        header.to_bytes(field_bytes);
        for (int i = 0; i < 13; i++) bytes[byte_idx + i] = field_bytes[i];
        byte_idx += 13;
      end
      begin
        logic [7:0] fb;
        fb = '0;
        fb[1:0] = mode;
        for (int i = 0; i < 1; i++) bytes[byte_idx + i] = fb[(1 - 1 - i)*8 +: 8];
        byte_idx += 1;
      end
      begin
        logic [7:0] fb;
        fb = '0;
        fb[2:0] = error_code;
        for (int i = 0; i < 1; i++) bytes[byte_idx + i] = fb[(1 - 1 - i)*8 +: 8];
        byte_idx += 1;
      end
      begin
        logic [31:0] fb;
        fb = '0;
        fb[31:0] = data1;
        for (int i = 0; i < 4; i++) bytes[byte_idx + i] = fb[(4 - 1 - i)*8 +: 8];
        byte_idx += 4;
      end
      begin
        logic [31:0] fb;
        fb = '0;
        fb[31:0] = data2;
        for (int i = 0; i < 4; i++) bytes[byte_idx + i] = fb[(4 - 1 - i)*8 +: 8];
        byte_idx += 4;
      end
    endtask
  
    function void from_bytes(input byte unsigned bytes[]);
      int byte_idx;
      if (bytes.size() != BYTE_COUNT) begin
        $fatal(1, "packet_ct.from_bytes size mismatch: expected %0d got %0d", BYTE_COUNT, bytes.size());
      end
      byte_idx = 0;
      begin
        byte unsigned field_bytes[] = new[13];
        for (int i = 0; i < 13; i++) field_bytes[i] = bytes[byte_idx + i];
        header.from_bytes(field_bytes);
        byte_idx += 13;
      end
      begin
        logic [7:0] fb;
        fb = '0;
        for (int i = 0; i < 1; i++) fb[(1 - 1 - i)*8 +: 8] = bytes[byte_idx + i];
        mode = fb[1:0];
        byte_idx += 1;
      end
      begin
        logic [7:0] fb;
        fb = '0;
        for (int i = 0; i < 1; i++) fb[(1 - 1 - i)*8 +: 8] = bytes[byte_idx + i];
        error_code = fb[2:0];
        byte_idx += 1;
      end
      begin
        logic [31:0] fb;
        fb = '0;
        for (int i = 0; i < 4; i++) fb[(4 - 1 - i)*8 +: 8] = bytes[byte_idx + i];
        data1 = fb[31:0];
        byte_idx += 4;
      end
      begin
        logic [31:0] fb;
        fb = '0;
        for (int i = 0; i < 4; i++) fb[(4 - 1 - i)*8 +: 8] = bytes[byte_idx + i];
        data2 = fb[31:0];
        byte_idx += 4;
      end
    endfunction
  
    function void copy(input packet_ct rhs);
      header.copy(rhs.header);
      mode = rhs.mode;
      error_code = rhs.error_code;
      data1 = rhs.data1;
      data2 = rhs.data2;
    endfunction
  
    function automatic packet_ct clone();
      packet_ct cloned = new();
      cloned.copy(this);
      return cloned;
    endfunction
  
    function automatic bit compare(input packet_ct rhs);
      bit match;
      match = 1'b1;
      match &= header.compare(rhs.header);
      match &= (mode === rhs.mode);
      match &= (error_code === rhs.error_code);
      match &= (data1 === rhs.data1);
      match &= (data2 === rhs.data2);
      return match;
    endfunction
  
    function automatic string sprint();
      return $sformatf("packet_ct(header=%s, mode=0x%0h, error_code=0x%0h, data1=0x%0h, data2=0x%0h)", header.sprint(), mode, error_code, data1, data2);
    endfunction
  endclass : packet_ct
endpackage
