// Generated by typist
// Source: alpha/typist/constants.py
// Do not edit by hand.

#ifndef FOO_BAR_CONSTANTS_TYPES_HPP
#define FOO_BAR_CONSTANTS_TYPES_HPP

#include <cstdint>
#include <cstddef>
#include <stdexcept>
#include <vector>

namespace foo::bar::constants {

constexpr std::int64_t FOO = 100000LL;
constexpr std::uint32_t BAR = 0U;
constexpr std::uint32_t A = 3U;
constexpr std::int32_t B = (A + 5);
constexpr std::int32_t C = ((A << 2) | 1);
constexpr std::int32_t D = (-A);
constexpr std::int32_t E = (~A);
constexpr std::int32_t W = 13;

class addr_ct {
 public:
  static constexpr std::size_t kWidth = 13;
  static constexpr bool kSigned = false;
  static constexpr std::size_t kByteCount = 2;
  using value_type = std::uint16_t;
  value_type value;
  static constexpr std::uint64_t kMask = 8191U;
  static constexpr value_type kMaxValue = static_cast<value_type>(8191U);
  addr_ct() : value(0) {}
  addr_ct(value_type value_in) : value(validate_value(value_in)) {}

  std::vector<std::uint8_t> to_bytes() const {
    std::vector<std::uint8_t> bytes(kByteCount, 0);
    std::uint64_t bits = static_cast<std::uint64_t>(value);
    for (std::size_t idx = 0; idx < kByteCount; ++idx) {
      bytes[kByteCount - 1 - idx] = static_cast<std::uint8_t>((bits >> (8U * idx)) & 0xFFU);
    }
    return bytes;
  }

  void from_bytes(const std::vector<std::uint8_t>& bytes) {
    if (bytes.size() != kByteCount) {
      throw std::invalid_argument("byte width mismatch");
    }
    std::uint64_t bits = 0;
    for (std::size_t idx = 0; idx < kByteCount; ++idx) {
      bits = (bits << 8U) | bytes[idx];
    }
    value = validate_value(static_cast<value_type>(bits & kMask));
  }

  addr_ct clone() const {
    return addr_ct(value);
  }

  operator value_type() const {
    return value;
  }

  bool operator==(const addr_ct& other) const = default;

 private:
  static value_type validate_value(value_type value_in) {
    if (value_in > kMaxValue) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }
};

class mask_ct {
 public:
  static constexpr std::size_t kWidth = 8;
  static constexpr bool kSigned = true;
  static constexpr std::size_t kByteCount = 1;
  using value_type = std::int8_t;
  value_type value;
  static constexpr value_type kMinValue = static_cast<value_type>(-128);
  static constexpr value_type kMaxValue = static_cast<value_type>(127);
  static constexpr std::uint64_t kMask = 255U;
  mask_ct() : value(0) {}
  mask_ct(value_type value_in) : value(validate_value(value_in)) {}

  std::vector<std::uint8_t> to_bytes() const {
    std::vector<std::uint8_t> bytes(kByteCount, 0);
    std::uint64_t bits = static_cast<std::uint64_t>(value) & kMask;
    for (std::size_t idx = 0; idx < kByteCount; ++idx) {
      bytes[kByteCount - 1 - idx] = static_cast<std::uint8_t>((bits >> (8U * idx)) & 0xFFU);
    }
    return bytes;
  }

  void from_bytes(const std::vector<std::uint8_t>& bytes) {
    if (bytes.size() != kByteCount) {
      throw std::invalid_argument("byte width mismatch");
    }
    std::uint64_t bits = 0;
    for (std::size_t idx = 0; idx < kByteCount; ++idx) {
      bits = (bits << 8U) | bytes[idx];
    }
    bits &= kMask;
    std::int64_t signed_value = static_cast<std::int64_t>(bits);
    if ((bits & 128U) != 0U && kWidth < 64) {
      signed_value -= static_cast<std::int64_t>(256U);
    }
    value = validate_value(static_cast<value_type>(signed_value));
  }

  mask_ct clone() const {
    return mask_ct(value);
  }

  operator value_type() const {
    return value;
  }

  bool operator==(const mask_ct& other) const = default;

 private:
  static value_type validate_value(value_type value_in) {
    if (value_in < kMinValue || value_in > kMaxValue) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }
};

class flag_ct {
 public:
  static constexpr std::size_t kWidth = 1;
  static constexpr bool kSigned = false;
  static constexpr std::size_t kByteCount = 1;
  using value_type = std::uint8_t;
  value_type value;
  static constexpr std::uint64_t kMask = 1U;
  static constexpr value_type kMaxValue = static_cast<value_type>(1U);
  flag_ct() : value(0) {}
  flag_ct(value_type value_in) : value(validate_value(value_in)) {}

  std::vector<std::uint8_t> to_bytes() const {
    std::vector<std::uint8_t> bytes(kByteCount, 0);
    std::uint64_t bits = static_cast<std::uint64_t>(value);
    for (std::size_t idx = 0; idx < kByteCount; ++idx) {
      bytes[kByteCount - 1 - idx] = static_cast<std::uint8_t>((bits >> (8U * idx)) & 0xFFU);
    }
    return bytes;
  }

  void from_bytes(const std::vector<std::uint8_t>& bytes) {
    if (bytes.size() != kByteCount) {
      throw std::invalid_argument("byte width mismatch");
    }
    std::uint64_t bits = 0;
    for (std::size_t idx = 0; idx < kByteCount; ++idx) {
      bits = (bits << 8U) | bytes[idx];
    }
    value = validate_value(static_cast<value_type>(bits & kMask));
  }

  flag_ct clone() const {
    return flag_ct(value);
  }

  operator value_type() const {
    return value;
  }

  bool operator==(const flag_ct& other) const = default;

 private:
  static value_type validate_value(value_type value_in) {
    if (value_in > kMaxValue) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }
};

class big_data_ct {
 public:
  static constexpr std::size_t kWidth = 67;
  static constexpr bool kSigned = false;
  static constexpr std::size_t kByteCount = 9;
  using value_type = std::vector<std::uint8_t>;
  value_type value;
  big_data_ct() : value(kByteCount, 0U) {}
  big_data_ct(const value_type& value_in) : value(validate_value(value_in)) {}

  std::vector<std::uint8_t> to_bytes() const {
    return value;
  }

  void from_bytes(const std::vector<std::uint8_t>& bytes) {
    value = validate_value(bytes);
  }

  big_data_ct clone() const {
    return big_data_ct(value);
  }

  bool operator==(const big_data_ct& other) const = default;

 private:
  static value_type validate_value(const value_type& value_in) {
    if (value_in.size() != kByteCount) {
      throw std::invalid_argument("byte width mismatch");
    }
    value_type normalized = value_in;
    normalized[0] &= 7U;
    return normalized;
  }
};

class header_ct {
 public:
  static constexpr std::size_t kWidth = 81;
  static constexpr std::size_t kByteCount = 12;
  addr_ct addr{};
  flag_ct enable{};
  big_data_ct data{};

  header_ct() = default;

  std::vector<std::uint8_t> to_bytes() const {
    std::vector<std::uint8_t> bytes;
    bytes.reserve(kByteCount);
    {
      auto field_bytes = addr.to_bytes();
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    {
      auto field_bytes = enable.to_bytes();
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    {
      auto field_bytes = data.to_bytes();
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    return bytes;
  }

  void from_bytes(const std::vector<std::uint8_t>& bytes) {
    if (bytes.size() != kByteCount) {
      throw std::invalid_argument("byte width mismatch");
    }
    std::size_t offset = 0;
    {
      std::vector<std::uint8_t> field_bytes(bytes.begin() + static_cast<std::ptrdiff_t>(offset), bytes.begin() + static_cast<std::ptrdiff_t>(offset + 2));
      addr.from_bytes(field_bytes);
      offset += 2;
    }
    {
      std::vector<std::uint8_t> field_bytes(bytes.begin() + static_cast<std::ptrdiff_t>(offset), bytes.begin() + static_cast<std::ptrdiff_t>(offset + 1));
      enable.from_bytes(field_bytes);
      offset += 1;
    }
    {
      std::vector<std::uint8_t> field_bytes(bytes.begin() + static_cast<std::ptrdiff_t>(offset), bytes.begin() + static_cast<std::ptrdiff_t>(offset + 9));
      data.from_bytes(field_bytes);
      offset += 9;
    }
  }

  header_ct clone() const {
    header_ct cloned;
    cloned.addr = addr.clone();
    cloned.enable = enable.clone();
    cloned.data = data.clone();
    return cloned;
  }

  bool operator==(const header_ct& other) const = default;
};

class packet_ct {
 public:
  static constexpr std::size_t kWidth = 150;
  static constexpr std::size_t kByteCount = 22;
  header_ct header{};
  std::uint8_t mode = 0;
  std::uint8_t error_code = 0;
  std::uint32_t data1 = 0;
  std::uint32_t data2 = 0;

  packet_ct() = default;

  std::vector<std::uint8_t> to_bytes() const {
    std::vector<std::uint8_t> bytes;
    bytes.reserve(kByteCount);
    {
      auto field_bytes = header.to_bytes();
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    {
      auto field_bytes = encode_mode(mode);
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    {
      auto field_bytes = encode_error_code(error_code);
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    {
      auto field_bytes = encode_data1(data1);
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    {
      auto field_bytes = encode_data2(data2);
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    return bytes;
  }

  void from_bytes(const std::vector<std::uint8_t>& bytes) {
    if (bytes.size() != kByteCount) {
      throw std::invalid_argument("byte width mismatch");
    }
    std::size_t offset = 0;
    {
      std::vector<std::uint8_t> field_bytes(bytes.begin() + static_cast<std::ptrdiff_t>(offset), bytes.begin() + static_cast<std::ptrdiff_t>(offset + 12));
      header.from_bytes(field_bytes);
      offset += 12;
    }
    mode = decode_mode(bytes, offset);
    offset += 1;
    error_code = decode_error_code(bytes, offset);
    offset += 1;
    data1 = decode_data1(bytes, offset);
    offset += 4;
    data2 = decode_data2(bytes, offset);
    offset += 4;
  }

  packet_ct clone() const {
    packet_ct cloned;
    cloned.header = header.clone();
    cloned.mode = mode;
    cloned.error_code = error_code;
    cloned.data1 = data1;
    cloned.data2 = data2;
    return cloned;
  }

  bool operator==(const packet_ct& other) const = default;

 private:
  static std::vector<std::uint8_t> encode_mode(std::uint8_t v) {
    validate_mode(v);
    std::vector<std::uint8_t> b(1, 0U);
    std::uint64_t bits = static_cast<std::uint64_t>(v);
    for (std::size_t i = 0; i < 1; ++i) {
      b[1 - 1 - i] = static_cast<std::uint8_t>((bits >> (8U * i)) & 0xFFU);
    }
    return b;
  }

  static std::uint8_t decode_mode(const std::vector<std::uint8_t>& bytes, std::size_t offset) {
    std::uint64_t bits = 0;
    for (std::size_t i = 0; i < 1; ++i) {
      bits = (bits << 8U) | bytes[offset + i];
    }
    bits &= 3U;
    return validate_mode(static_cast<std::uint8_t>(bits));
  }

  static std::uint8_t validate_mode(std::uint8_t value_in) {
    constexpr std::uint8_t kMaxValue = static_cast<std::uint8_t>(3U);
    if (value_in > kMaxValue) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }

  static std::vector<std::uint8_t> encode_error_code(std::uint8_t v) {
    validate_error_code(v);
    std::vector<std::uint8_t> b(1, 0U);
    std::uint64_t bits = static_cast<std::uint64_t>(v);
    for (std::size_t i = 0; i < 1; ++i) {
      b[1 - 1 - i] = static_cast<std::uint8_t>((bits >> (8U * i)) & 0xFFU);
    }
    return b;
  }

  static std::uint8_t decode_error_code(const std::vector<std::uint8_t>& bytes, std::size_t offset) {
    std::uint64_t bits = 0;
    for (std::size_t i = 0; i < 1; ++i) {
      bits = (bits << 8U) | bytes[offset + i];
    }
    bits &= 7U;
    return validate_error_code(static_cast<std::uint8_t>(bits));
  }

  static std::uint8_t validate_error_code(std::uint8_t value_in) {
    constexpr std::uint8_t kMaxValue = static_cast<std::uint8_t>(7U);
    if (value_in > kMaxValue) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }

  static std::vector<std::uint8_t> encode_data1(std::uint32_t v) {
    validate_data1(v);
    std::vector<std::uint8_t> b(4, 0U);
    std::uint64_t bits = static_cast<std::uint64_t>(v);
    for (std::size_t i = 0; i < 4; ++i) {
      b[4 - 1 - i] = static_cast<std::uint8_t>((bits >> (8U * i)) & 0xFFU);
    }
    return b;
  }

  static std::uint32_t decode_data1(const std::vector<std::uint8_t>& bytes, std::size_t offset) {
    std::uint64_t bits = 0;
    for (std::size_t i = 0; i < 4; ++i) {
      bits = (bits << 8U) | bytes[offset + i];
    }
    return validate_data1(static_cast<std::uint32_t>(bits));
  }

  static std::uint32_t validate_data1(std::uint32_t value_in) {
    constexpr std::uint32_t kMaxValue = static_cast<std::uint32_t>(4294967295U);
    if (value_in > kMaxValue) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }

  static std::vector<std::uint8_t> encode_data2(std::uint32_t v) {
    validate_data2(v);
    std::vector<std::uint8_t> b(4, 0U);
    std::uint64_t bits = static_cast<std::uint64_t>(v);
    for (std::size_t i = 0; i < 4; ++i) {
      b[4 - 1 - i] = static_cast<std::uint8_t>((bits >> (8U * i)) & 0xFFU);
    }
    return b;
  }

  static std::uint32_t decode_data2(const std::vector<std::uint8_t>& bytes, std::size_t offset) {
    std::uint64_t bits = 0;
    for (std::size_t i = 0; i < 4; ++i) {
      bits = (bits << 8U) | bytes[offset + i];
    }
    return validate_data2(static_cast<std::uint32_t>(bits));
  }

  static std::uint32_t validate_data2(std::uint32_t value_in) {
    constexpr std::uint32_t kMaxValue = static_cast<std::uint32_t>(4294967295U);
    if (value_in > kMaxValue) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }
};

}  // namespace foo::bar::constants

#endif  // FOO_BAR_CONSTANTS_TYPES_HPP
