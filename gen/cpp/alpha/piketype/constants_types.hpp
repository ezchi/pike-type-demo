// Generated by piketype
// Source: alpha/piketype/constants.py
// Do not edit by hand.

#ifndef ALPHA_PIKETYPE_CONSTANTS_TYPES_HPP
#define ALPHA_PIKETYPE_CONSTANTS_TYPES_HPP

#include <cstdint>
#include <cstddef>
#include <stdexcept>
#include <vector>

namespace alpha::constants {

constexpr std::int64_t FOO = 100000LL;
constexpr std::uint32_t BAR = 0U;
constexpr std::uint32_t A = 3U;
constexpr std::int32_t B = (A + 5);
constexpr std::int32_t C = ((A << 2) | 1);
constexpr std::int32_t D = (-A);
constexpr std::int32_t E = (~A);
constexpr std::int32_t W = 13;

class addr_ct {
 public:
  static constexpr std::size_t WIDTH = 13;
  static constexpr bool SIGNED = false;
  static constexpr std::size_t BYTE_COUNT = 2;
  using value_type = std::uint16_t;
  value_type value;
  static constexpr std::uint64_t MASK = 8191U;
  static constexpr value_type MAX_VALUE = static_cast<value_type>(8191U);
  addr_ct() : value(0) {}
  addr_ct(value_type value_in) : value(validate_value(value_in)) {}

  std::vector<std::uint8_t> to_bytes() const {
    std::vector<std::uint8_t> bytes(BYTE_COUNT, 0);
    std::uint64_t bits = static_cast<std::uint64_t>(value);
    for (std::size_t idx = 0; idx < BYTE_COUNT; ++idx) {
      bytes[BYTE_COUNT - 1 - idx] = static_cast<std::uint8_t>((bits >> (8U * idx)) & 0xFFU);
    }
    return bytes;
  }

  void from_bytes(const std::vector<std::uint8_t>& bytes) {
    if (bytes.size() != BYTE_COUNT) {
      throw std::invalid_argument("byte width mismatch");
    }
    std::uint64_t bits = 0;
    for (std::size_t idx = 0; idx < BYTE_COUNT; ++idx) {
      bits = (bits << 8U) | bytes[idx];
    }
    value = validate_value(static_cast<value_type>(bits & MASK));
  }

  addr_ct clone() const {
    return addr_ct(value);
  }

  operator value_type() const {
    return value;
  }

  bool operator==(const addr_ct& other) const = default;

 private:
  static value_type validate_value(value_type value_in) {
    if (value_in > MAX_VALUE) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }
};

class mask_ct {
 public:
  static constexpr std::size_t WIDTH = 8;
  static constexpr bool SIGNED = true;
  static constexpr std::size_t BYTE_COUNT = 1;
  using value_type = std::int8_t;
  value_type value;
  static constexpr value_type MIN_VALUE = static_cast<value_type>(-128);
  static constexpr value_type MAX_VALUE = static_cast<value_type>(127);
  static constexpr std::uint64_t MASK = 255U;
  mask_ct() : value(0) {}
  mask_ct(value_type value_in) : value(validate_value(value_in)) {}

  std::vector<std::uint8_t> to_bytes() const {
    std::vector<std::uint8_t> bytes(BYTE_COUNT, 0);
    std::uint64_t bits = static_cast<std::uint64_t>(value) & MASK;
    for (std::size_t idx = 0; idx < BYTE_COUNT; ++idx) {
      bytes[BYTE_COUNT - 1 - idx] = static_cast<std::uint8_t>((bits >> (8U * idx)) & 0xFFU);
    }
    return bytes;
  }

  void from_bytes(const std::vector<std::uint8_t>& bytes) {
    if (bytes.size() != BYTE_COUNT) {
      throw std::invalid_argument("byte width mismatch");
    }
    std::uint64_t bits = 0;
    for (std::size_t idx = 0; idx < BYTE_COUNT; ++idx) {
      bits = (bits << 8U) | bytes[idx];
    }
    bits &= MASK;
    std::int64_t signed_value = static_cast<std::int64_t>(bits);
    if ((bits & 128U) != 0U && WIDTH < 64) {
      signed_value -= static_cast<std::int64_t>(256U);
    }
    value = validate_value(static_cast<value_type>(signed_value));
  }

  mask_ct clone() const {
    return mask_ct(value);
  }

  operator value_type() const {
    return value;
  }

  bool operator==(const mask_ct& other) const = default;

 private:
  static value_type validate_value(value_type value_in) {
    if (value_in < MIN_VALUE || value_in > MAX_VALUE) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }
};

class flag_ct {
 public:
  static constexpr std::size_t WIDTH = 1;
  static constexpr bool SIGNED = false;
  static constexpr std::size_t BYTE_COUNT = 1;
  using value_type = std::uint8_t;
  value_type value;
  static constexpr std::uint64_t MASK = 1U;
  static constexpr value_type MAX_VALUE = static_cast<value_type>(1U);
  flag_ct() : value(0) {}
  flag_ct(value_type value_in) : value(validate_value(value_in)) {}

  std::vector<std::uint8_t> to_bytes() const {
    std::vector<std::uint8_t> bytes(BYTE_COUNT, 0);
    std::uint64_t bits = static_cast<std::uint64_t>(value);
    for (std::size_t idx = 0; idx < BYTE_COUNT; ++idx) {
      bytes[BYTE_COUNT - 1 - idx] = static_cast<std::uint8_t>((bits >> (8U * idx)) & 0xFFU);
    }
    return bytes;
  }

  void from_bytes(const std::vector<std::uint8_t>& bytes) {
    if (bytes.size() != BYTE_COUNT) {
      throw std::invalid_argument("byte width mismatch");
    }
    std::uint64_t bits = 0;
    for (std::size_t idx = 0; idx < BYTE_COUNT; ++idx) {
      bits = (bits << 8U) | bytes[idx];
    }
    value = validate_value(static_cast<value_type>(bits & MASK));
  }

  flag_ct clone() const {
    return flag_ct(value);
  }

  operator value_type() const {
    return value;
  }

  bool operator==(const flag_ct& other) const = default;

 private:
  static value_type validate_value(value_type value_in) {
    if (value_in > MAX_VALUE) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }
};

class big_data_ct {
 public:
  static constexpr std::size_t WIDTH = 67;
  static constexpr bool SIGNED = false;
  static constexpr std::size_t BYTE_COUNT = 9;
  using value_type = std::vector<std::uint8_t>;
  value_type value;
  big_data_ct() : value(BYTE_COUNT, 0U) {}
  big_data_ct(const value_type& value_in) : value(validate_value(value_in)) {}

  std::vector<std::uint8_t> to_bytes() const {
    return value;
  }

  void from_bytes(const std::vector<std::uint8_t>& bytes) {
    value = validate_value(bytes);
  }

  big_data_ct clone() const {
    return big_data_ct(value);
  }

  bool operator==(const big_data_ct& other) const = default;

 private:
  static value_type validate_value(const value_type& value_in) {
    if (value_in.size() != BYTE_COUNT) {
      throw std::invalid_argument("byte width mismatch");
    }
    value_type normalized = value_in;
    normalized[0] &= 7U;
    return normalized;
  }
};

class flags_ct {
 public:
  static constexpr std::size_t WIDTH = 3;
  static constexpr std::size_t BYTE_COUNT = 1;
  using value_type = std::uint8_t;
  static constexpr value_type INVALID_DATA_MASK = 0x80U;
  static constexpr value_type TIMEOUT_MASK = 0x40U;
  static constexpr value_type OVERFLOW_MASK = 0x20U;
  value_type value = 0;

  flags_ct() = default;

  bool get_invalid_data() const { return (value & INVALID_DATA_MASK) != 0; }
  void set_invalid_data(bool v) { if (v) value |= INVALID_DATA_MASK; else value &= static_cast<value_type>(~INVALID_DATA_MASK); }

  bool get_timeout() const { return (value & TIMEOUT_MASK) != 0; }
  void set_timeout(bool v) { if (v) value |= TIMEOUT_MASK; else value &= static_cast<value_type>(~TIMEOUT_MASK); }

  bool get_overflow() const { return (value & OVERFLOW_MASK) != 0; }
  void set_overflow(bool v) { if (v) value |= OVERFLOW_MASK; else value &= static_cast<value_type>(~OVERFLOW_MASK); }

  std::vector<std::uint8_t> to_bytes() const {
    std::vector<std::uint8_t> bytes(BYTE_COUNT, 0);
    value_type masked = value & 0xE0U;
    for (std::size_t idx = 0; idx < BYTE_COUNT; ++idx) {
      bytes[BYTE_COUNT - 1 - idx] = static_cast<std::uint8_t>((static_cast<std::uint64_t>(masked) >> (8U * idx)) & 0xFFU);
    }
    return bytes;
  }

  void from_bytes(const std::vector<std::uint8_t>& bytes) {
    if (bytes.size() != BYTE_COUNT) {
      throw std::invalid_argument("byte width mismatch");
    }
    std::uint64_t bits = 0;
    for (std::size_t idx = 0; idx < BYTE_COUNT; ++idx) {
      bits = (bits << 8U) | bytes[idx];
    }
    value = static_cast<value_type>(bits) & 0xE0U;
  }

  flags_ct clone() const {
    flags_ct cloned;
    cloned.value = value;
    return cloned;
  }

  bool operator==(const flags_ct& other) const {
    return (value & 0xE0U) == (other.value & 0xE0U);
  }
};

class header_ct {
 public:
  static constexpr std::size_t WIDTH = 84;
  static constexpr std::size_t BYTE_COUNT = 13;
  addr_ct addr{};
  flag_ct enable{};
  big_data_ct data{};
  flags_ct status{};

  header_ct() = default;

  std::vector<std::uint8_t> to_bytes() const {
    std::vector<std::uint8_t> bytes;
    bytes.reserve(BYTE_COUNT);
    {
      auto field_bytes = addr.to_bytes();
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    {
      auto field_bytes = enable.to_bytes();
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    {
      auto field_bytes = data.to_bytes();
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    {
      auto field_bytes = status.to_bytes();
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    return bytes;
  }

  void from_bytes(const std::vector<std::uint8_t>& bytes) {
    if (bytes.size() != BYTE_COUNT) {
      throw std::invalid_argument("byte width mismatch");
    }
    std::size_t offset = 0;
    {
      std::vector<std::uint8_t> field_bytes(bytes.begin() + static_cast<std::ptrdiff_t>(offset), bytes.begin() + static_cast<std::ptrdiff_t>(offset + 2));
      addr.from_bytes(field_bytes);
      offset += 2;
    }
    {
      std::vector<std::uint8_t> field_bytes(bytes.begin() + static_cast<std::ptrdiff_t>(offset), bytes.begin() + static_cast<std::ptrdiff_t>(offset + 1));
      enable.from_bytes(field_bytes);
      offset += 1;
    }
    {
      std::vector<std::uint8_t> field_bytes(bytes.begin() + static_cast<std::ptrdiff_t>(offset), bytes.begin() + static_cast<std::ptrdiff_t>(offset + 9));
      data.from_bytes(field_bytes);
      offset += 9;
    }
    {
      std::vector<std::uint8_t> field_bytes(bytes.begin() + static_cast<std::ptrdiff_t>(offset), bytes.begin() + static_cast<std::ptrdiff_t>(offset + 1));
      status.from_bytes(field_bytes);
      offset += 1;
    }
  }

  header_ct clone() const {
    header_ct cloned;
    cloned.addr = addr.clone();
    cloned.enable = enable.clone();
    cloned.data = data.clone();
    cloned.status = status.clone();
    return cloned;
  }

  bool operator==(const header_ct& other) const = default;
};

class packet_ct {
 public:
  static constexpr std::size_t WIDTH = 153;
  static constexpr std::size_t BYTE_COUNT = 23;
  header_ct header{};
  std::uint8_t mode = 0;
  std::uint8_t error_code = 0;
  std::uint32_t data1 = 0;
  std::uint32_t data2 = 0;

  packet_ct() = default;

  std::vector<std::uint8_t> to_bytes() const {
    std::vector<std::uint8_t> bytes;
    bytes.reserve(BYTE_COUNT);
    {
      auto field_bytes = header.to_bytes();
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    {
      auto field_bytes = encode_mode(mode);
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    {
      auto field_bytes = encode_error_code(error_code);
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    {
      auto field_bytes = encode_data1(data1);
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    {
      auto field_bytes = encode_data2(data2);
      bytes.insert(bytes.end(), field_bytes.begin(), field_bytes.end());
    }
    return bytes;
  }

  void from_bytes(const std::vector<std::uint8_t>& bytes) {
    if (bytes.size() != BYTE_COUNT) {
      throw std::invalid_argument("byte width mismatch");
    }
    std::size_t offset = 0;
    {
      std::vector<std::uint8_t> field_bytes(bytes.begin() + static_cast<std::ptrdiff_t>(offset), bytes.begin() + static_cast<std::ptrdiff_t>(offset + 13));
      header.from_bytes(field_bytes);
      offset += 13;
    }
    mode = decode_mode(bytes, offset);
    offset += 1;
    error_code = decode_error_code(bytes, offset);
    offset += 1;
    data1 = decode_data1(bytes, offset);
    offset += 4;
    data2 = decode_data2(bytes, offset);
    offset += 4;
  }

  packet_ct clone() const {
    packet_ct cloned;
    cloned.header = header.clone();
    cloned.mode = mode;
    cloned.error_code = error_code;
    cloned.data1 = data1;
    cloned.data2 = data2;
    return cloned;
  }

  bool operator==(const packet_ct& other) const = default;

 private:
  static std::vector<std::uint8_t> encode_mode(std::uint8_t v) {
    validate_mode(v);
    std::vector<std::uint8_t> b(1, 0U);
    std::uint64_t bits = static_cast<std::uint64_t>(v);
    for (std::size_t i = 0; i < 1; ++i) {
      b[1 - 1 - i] = static_cast<std::uint8_t>((bits >> (8U * i)) & 0xFFU);
    }
    return b;
  }

  static std::uint8_t decode_mode(const std::vector<std::uint8_t>& bytes, std::size_t offset) {
    std::uint64_t bits = 0;
    for (std::size_t i = 0; i < 1; ++i) {
      bits = (bits << 8U) | bytes[offset + i];
    }
    bits &= 3U;
    return validate_mode(static_cast<std::uint8_t>(bits));
  }

  static std::uint8_t validate_mode(std::uint8_t value_in) {
    constexpr std::uint8_t MAX_VALUE = static_cast<std::uint8_t>(3U);
    if (value_in > MAX_VALUE) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }

  static std::vector<std::uint8_t> encode_error_code(std::uint8_t v) {
    validate_error_code(v);
    std::vector<std::uint8_t> b(1, 0U);
    std::uint64_t bits = static_cast<std::uint64_t>(v);
    for (std::size_t i = 0; i < 1; ++i) {
      b[1 - 1 - i] = static_cast<std::uint8_t>((bits >> (8U * i)) & 0xFFU);
    }
    return b;
  }

  static std::uint8_t decode_error_code(const std::vector<std::uint8_t>& bytes, std::size_t offset) {
    std::uint64_t bits = 0;
    for (std::size_t i = 0; i < 1; ++i) {
      bits = (bits << 8U) | bytes[offset + i];
    }
    bits &= 7U;
    return validate_error_code(static_cast<std::uint8_t>(bits));
  }

  static std::uint8_t validate_error_code(std::uint8_t value_in) {
    constexpr std::uint8_t MAX_VALUE = static_cast<std::uint8_t>(7U);
    if (value_in > MAX_VALUE) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }

  static std::vector<std::uint8_t> encode_data1(std::uint32_t v) {
    validate_data1(v);
    std::vector<std::uint8_t> b(4, 0U);
    std::uint64_t bits = static_cast<std::uint64_t>(v);
    for (std::size_t i = 0; i < 4; ++i) {
      b[4 - 1 - i] = static_cast<std::uint8_t>((bits >> (8U * i)) & 0xFFU);
    }
    return b;
  }

  static std::uint32_t decode_data1(const std::vector<std::uint8_t>& bytes, std::size_t offset) {
    std::uint64_t bits = 0;
    for (std::size_t i = 0; i < 4; ++i) {
      bits = (bits << 8U) | bytes[offset + i];
    }
    return validate_data1(static_cast<std::uint32_t>(bits));
  }

  static std::uint32_t validate_data1(std::uint32_t value_in) {
    constexpr std::uint32_t MAX_VALUE = static_cast<std::uint32_t>(4294967295U);
    if (value_in > MAX_VALUE) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }

  static std::vector<std::uint8_t> encode_data2(std::uint32_t v) {
    validate_data2(v);
    std::vector<std::uint8_t> b(4, 0U);
    std::uint64_t bits = static_cast<std::uint64_t>(v);
    for (std::size_t i = 0; i < 4; ++i) {
      b[4 - 1 - i] = static_cast<std::uint8_t>((bits >> (8U * i)) & 0xFFU);
    }
    return b;
  }

  static std::uint32_t decode_data2(const std::vector<std::uint8_t>& bytes, std::size_t offset) {
    std::uint64_t bits = 0;
    for (std::size_t i = 0; i < 4; ++i) {
      bits = (bits << 8U) | bytes[offset + i];
    }
    return validate_data2(static_cast<std::uint32_t>(bits));
  }

  static std::uint32_t validate_data2(std::uint32_t value_in) {
    constexpr std::uint32_t MAX_VALUE = static_cast<std::uint32_t>(4294967295U);
    if (value_in > MAX_VALUE) {
      throw std::out_of_range("value out of range");
    }
    return value_in;
  }
};

}  // namespace alpha::constants

#endif  // ALPHA_PIKETYPE_CONSTANTS_TYPES_HPP
