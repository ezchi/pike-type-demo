# Generated by typist
# Source: alpha/typist/constants.py
# Do not edit by hand.

from __future__ import annotations

FOO = 100000
BAR = 0
A = 3
B = (A + 5)
C = ((A << 2) | 1)
D = (-A)
E = (~A)
W = 13

class addr_ct:
    WIDTH = 13
    SIGNED = False
    BYTE_COUNT = 2
    MIN_VALUE = 0
    MAX_VALUE = 8191

    def __init__(self, value: int = 0) -> None:
        if not isinstance(value, int):
            raise TypeError("addr_ct value must be int")
        if value < self.MIN_VALUE or value > self.MAX_VALUE:
            raise ValueError("addr_ct value out of range")
        self.value = value

    def to_bytes(self) -> bytes:
        return self.value.to_bytes(self.BYTE_COUNT, "little", signed=False)

    @classmethod
    def from_bytes(cls, data: bytes | bytearray) -> "addr_ct":
        if not isinstance(data, (bytes, bytearray)):
            raise TypeError("addr_ct.from_bytes expects bytes or bytearray")
        raw = bytes(data)
        if len(raw) != cls.BYTE_COUNT:
            raise ValueError("addr_ct.from_bytes size mismatch")
        return cls(int.from_bytes(raw, "little", signed=False))

    def clone(self) -> "addr_ct":
        return type(self)(self.value)

    def __int__(self) -> int:
        return self.value

    def __index__(self) -> int:
        return self.value

    def __eq__(self, other: object) -> bool:
        if isinstance(other, type(self)):
            return self.value == other.value
        if isinstance(other, int):
            return self.value == other
        return NotImplemented

    def __repr__(self) -> str:
        return f"addr_ct(value={self.value!r})"

class mask_ct:
    WIDTH = 8
    SIGNED = True
    BYTE_COUNT = 1
    MIN_VALUE = -128
    MAX_VALUE = 127
    MASK = 255
    SIGN_BIT = 128

    def __init__(self, value: int = 0) -> None:
        if not isinstance(value, int):
            raise TypeError("mask_ct value must be int")
        if value < self.MIN_VALUE or value > self.MAX_VALUE:
            raise ValueError("mask_ct value out of range")
        self.value = value

    def to_bytes(self) -> bytes:
        return (self.value & self.MASK).to_bytes(self.BYTE_COUNT, "little", signed=False)

    @classmethod
    def from_bytes(cls, data: bytes | bytearray) -> "mask_ct":
        if not isinstance(data, (bytes, bytearray)):
            raise TypeError("mask_ct.from_bytes expects bytes or bytearray")
        raw = bytes(data)
        if len(raw) != cls.BYTE_COUNT:
            raise ValueError("mask_ct.from_bytes size mismatch")
        value = int.from_bytes(raw, "little", signed=False)
        if value > cls.MASK:
            raise ValueError("mask_ct.from_bytes value out of range")
        signed_value = value - (1 << cls.WIDTH) if (value & cls.SIGN_BIT) else value
        return cls(signed_value)

    def clone(self) -> "mask_ct":
        return type(self)(self.value)

    def __int__(self) -> int:
        return self.value

    def __index__(self) -> int:
        return self.value

    def __eq__(self, other: object) -> bool:
        if isinstance(other, type(self)):
            return self.value == other.value
        if isinstance(other, int):
            return self.value == other
        return NotImplemented

    def __repr__(self) -> str:
        return f"mask_ct(value={self.value!r})"

class flag_ct:
    WIDTH = 1
    SIGNED = False
    BYTE_COUNT = 1
    MIN_VALUE = 0
    MAX_VALUE = 1

    def __init__(self, value: int = 0) -> None:
        if not isinstance(value, int):
            raise TypeError("flag_ct value must be int")
        if value < self.MIN_VALUE or value > self.MAX_VALUE:
            raise ValueError("flag_ct value out of range")
        self.value = value

    def to_bytes(self) -> bytes:
        return self.value.to_bytes(self.BYTE_COUNT, "little", signed=False)

    @classmethod
    def from_bytes(cls, data: bytes | bytearray) -> "flag_ct":
        if not isinstance(data, (bytes, bytearray)):
            raise TypeError("flag_ct.from_bytes expects bytes or bytearray")
        raw = bytes(data)
        if len(raw) != cls.BYTE_COUNT:
            raise ValueError("flag_ct.from_bytes size mismatch")
        return cls(int.from_bytes(raw, "little", signed=False))

    def clone(self) -> "flag_ct":
        return type(self)(self.value)

    def __int__(self) -> int:
        return self.value

    def __index__(self) -> int:
        return self.value

    def __eq__(self, other: object) -> bool:
        if isinstance(other, type(self)):
            return self.value == other.value
        if isinstance(other, int):
            return self.value == other
        return NotImplemented

    def __repr__(self) -> str:
        return f"flag_ct(value={self.value!r})"

class big_data_ct:
    WIDTH = 67
    SIGNED = False
    BYTE_COUNT = 9
    MAX_VALUE = 147573952589676412927

    def __init__(self, value: bytes | bytearray | int = b'') -> None:
        if isinstance(value, int):
            if value < 0 or value > self.MAX_VALUE:
                raise ValueError("big_data_ct value out of range")
            self.value = value.to_bytes(self.BYTE_COUNT, "little", signed=False)
            return
        if not isinstance(value, (bytes, bytearray)):
            raise TypeError("big_data_ct value must be bytes, bytearray, or int")
        raw = bytes(value)
        if len(raw) == 0:
            raw = b"\x00" * self.BYTE_COUNT
        if len(raw) != self.BYTE_COUNT:
            raise ValueError("big_data_ct value size mismatch")
        self.value = raw

    def to_bytes(self) -> bytes:
        return self.value

    @classmethod
    def from_bytes(cls, data: bytes | bytearray) -> "big_data_ct":
        if not isinstance(data, (bytes, bytearray)):
            raise TypeError("big_data_ct.from_bytes expects bytes or bytearray")
        return cls(bytes(data))

    def clone(self) -> "big_data_ct":
        return type(self)(self.value)

    def __eq__(self, other: object) -> bool:
        if isinstance(other, type(self)):
            return self.value == other.value
        if isinstance(other, (bytes, bytearray)):
            return self.value == bytes(other)
        return NotImplemented

    def __repr__(self) -> str:
        return f"big_data_ct(value={self.value!r})"
